#' @param lat \code{numeric}. A valid latitude in decimal degrees
#' @param lng \code{numeric}. A valid longitude in decimal degrees
#' @param sf \code{logical}. Should the results be returned as an `sf` object (defaults to `TRUE`)? If `FALSE`, a `data.frame` is returned.
#' @param shape \code{NA}. A valid WKT geometry
#' @param sample \code{NA}. Get a sample response
