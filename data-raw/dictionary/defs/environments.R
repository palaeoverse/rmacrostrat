#' @param environ \code{character}. The name of a specific environment (e.g. reef).
#' @param environ_type \code{character}. The name of a type of environment (e.g. carbonate).
#' @param environ_class \code{character}. The name of a class of envrionment (e.g. marine).
#' @param id \code{integer}. The unique identification number(s) of the desired environment(s).
#' @param all \code{logical}. return all environment definitions
