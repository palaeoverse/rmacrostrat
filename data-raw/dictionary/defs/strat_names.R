#' @param strat_name \code{character}. The name(s) of the desired stratigraphic unit(s) to return a definition for.
#' @param interval_name \code{character}. The chronostratigraphic interval name for which to return stratigraphic name definitions with any temporal overlap (e.g., "Campanian"). This returns only `strat_names` with an age declared in Macrostrat.
#' @param rank \code{character}. The formal stratigraphic rank abbreviation of the desired unit(s) (i.e., "SGp", "Gp", "SubGp", "Fm", "Mbr", or "Bed") to return a definition for.
#' @param concept_id \code{integer}. The unique identification number(s) of stratigraphic concepts (grouped `strat_name`s which represent the same entity) to return a definition for.
#' @param ref_id \code{character}. The unique identification number(s) of the desired reference(s). Only stratigraphic names linked to the specified `ref_id` will be returned.
#' @param strat_name_id \code{integer}. The unique identification number(s) of the desired stratigraphic name to return a definition for.
#' @param rule \code{character}. The hierarchical rule for returning stratigraphic name definitions (either "all" or "down"). The "all" option will return the entire stratigraphic name hierarchy (parents and children of all levels) that the specified request belongs to. The "down" option will return any direct children of the requested stratigraphic name. Can only be used with `strat_name_id`.
