#' @param source_id \code{NA}. integer, one or more comma-separated source IDs
#' @param scale \code{NA}. string, one or more comma-separated Burwell scales (tiny, small, medium, large)
#' @param lat \code{NA}. A valid latitude
#' @param lng \code{NA}. A valid longitude
#' @param shape \code{NA}. string, a WKT geometry that can be used to filter sources
#' @param buffer \code{NA}. integer, buffer in meters that should be applied to a provided shape
#' @param all \code{NA}. return all source definitions
#' @param sf \code{NA}. Should the results be returned as an `sf` object (defaults to TRUE)?If `FALSE`, a `data.frame` is returned.
