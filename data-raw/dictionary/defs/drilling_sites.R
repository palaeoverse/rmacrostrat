#' @param epoch \code{character}. The name of drilling 'epoch'. Either: "DSDP", "ODP", or "IODP".
#' @param leg \code{integer}. The unique identification number(s) of drilling leg(s).
#' @param site \code{integer}. The unique identification number(s) of drilling site(s).
#' @param col_id \code{integer}. The unique identification number(s) of Macrostrat column(s).
#' @param col_group_id \code{integer}. The unique identification number(s) of Macrostrat column group(s).
#' @param sf \code{logical}. Should the results be returned as an `sf` object (defaults to `TRUE`)? If `FALSE`, a `data.frame` is returned.
#' @param all \code{logical}. return all drilling expeditions and sites
#' @param sample \code{logical}. if present, get a selection of data
