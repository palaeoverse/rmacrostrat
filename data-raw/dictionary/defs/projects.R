#' @param project_id \code{integer}. The unique identification number(s) of the desired project(s). If NULL (default), all projects are returned.
#' @param all \code{logical}. Should all available projects be returned?
