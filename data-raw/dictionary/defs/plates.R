#' @param plate_id \code{integer}. The unique identification number(s) of the desired plate(s). If NULL (default), all plates are returned.
#' @param all \code{logical}. Should all available plates be returned?
