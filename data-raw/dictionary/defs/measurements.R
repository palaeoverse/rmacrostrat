#' @param measure_id \code{integer}. The unique identification number(s) of the desired measurement definition(s) to return a definition for.
#' @param measurement_type \code{character}. The type of measurement definitions to return a definition for (e.g., "geochronological"). This is a less inclusive grouping than `class`.
#' @param measurement_class \code{character}. The class of measurement definitions to return a definition for (e.g., "geochemical"). This is a more inclusive grouping than `type`.
