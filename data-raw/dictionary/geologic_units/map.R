#' @param scale \code{NA}. Can be 'small', 'medium', or 'large'
#' @param map_id \code{NA}. integer, one or more polygon map_ids to search for
#' @param lat \code{NA}. A valid latitude in decimal degrees
#' @param lng \code{NA}. A valid longitude in decimal degrees
#' @param strat_name_id \code{NA}. integer, one or more valid strat_name_ids from /defs/strat_names
#' @param unit_id \code{NA}. integer, one or more valid unit_ids from /units
#' @param sf \code{NA}. Should the results be returned as an `sf` object (defaults to TRUE)?If `FALSE`, a `data.frame` is returned.
