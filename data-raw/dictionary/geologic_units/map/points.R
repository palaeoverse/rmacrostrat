#' @param point_id \code{NA}. One or more comma-separated valid point_ids
#' @param point_type \code{NA}. One or more comma-separated point_types
#' @param certainty \code{NA}. A string to search the certainty field for (fuzzy)
#' @param comments \code{NA}. A string to search the comments field for (fuzzy)
#' @param source_id \code{NA}. One or more comma-separated source_ids
#' @param minlat \code{NA}. A minimum latitude that represents the southwest corner of a bounding box (requires minlng, maxlat, and maxlng)
#' @param minlng \code{NA}. A minimum longitude that represents the southwest corner of a bounding box (requires minlat, maxlat, and maxlng)
#' @param maxlat \code{NA}. A maximum latitude that represents the northeast corner of a bounding box (requires minlat, minlng, and maxlng)
#' @param maxlng \code{NA}. A maximum longitude that represents the northeast corner of a bounding box (requires minlat, minlng, and maxlat)
#' @param sample \code{NA}. Return a sample of data
#' @param sf \code{NA}. Should the results be returned as an `sf` object (defaults to TRUE)?If `FALSE`, a `data.frame` is returned.
