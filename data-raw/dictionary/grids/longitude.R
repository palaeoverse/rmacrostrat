#' @param latSpacing \code{NA}. the number of degrees between bands of latitude
#' @param cellArea \code{NA}. target cell area in km^2
#' @param sf \code{NA}. Should the results be returned as an `sf` object (defaults to TRUE)?If `FALSE`, a `data.frame` is returned.
